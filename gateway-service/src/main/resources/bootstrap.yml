server:
  port: 15000
spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins: "*"
            allowed-headers: "*"
            allowed-methods:
              - GET
              - POST
              - PUT
              - DELETE
      routes:
        - id: blog-service
          uri: lb://blog-service
          predicates:
            - Path=/blog-service/**


  profiles:
    active: dev
